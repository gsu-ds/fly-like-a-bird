## Quick Start

### 1. Install Dependencies

```bash
pip install -r flight_status_prediction/app/app_requirements.txt
```

### 2. Run the Dashboard

```bash
streamlit run flight_status_prediction/app/app.py
```

The dashboard will automatically open in your default web browser at `http://localhost:8501`
